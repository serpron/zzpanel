<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zz.dao.TreeNodeMapper">
    <select id="findAllDepartments" resultType="zz.entity.TreeNode">
        select id,name,parent_id parent,parent_ids from sys_department order by id
    </select>
    <select id="findAllResources" resultType="zz.entity.TreeNode">
        select id,menu_name name,parent_id parent,parent_ids from sys_resource order by id
    </select>
    <select id="findAllDepartmentsWithUsers" resultType="zz.entity.TreeNode">
        select * from (
        select id,name,parent_id parent,parent_ids from sys_department
        union
        select concat(department_id,'_',id) id,name,department_id parent,'' parent_ids from sys_user)A order by A.id
    </select>
</mapper>